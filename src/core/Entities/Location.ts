/**
   Restaurant or Pub Location in latitude and longitude.
   */
export class ROPLocation {
  lat: number;
  long: number;

  constructor(lat: number, long: number) {
    this.lat = lat;
    this.long = long;
  }
}
